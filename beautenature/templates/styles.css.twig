{% block css %}
    <style>
        .nav-link {
            padding: 1.5rem 2rem;
        }

        .bg-primary {
            text-align: center;
            font-weight: bold;
            background-color: #fd8000;
            color: #fff;
            font-style: italic;
            margin-bottom: 3%;
            display: flex;
            position:relative;
        }

        .container-fluid {
            width: auto;
            padding: 1.5rem 2rem;
            margin: auto;
        }
    </style>
{% endblock %}